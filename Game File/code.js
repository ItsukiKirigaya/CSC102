function updateText() {
    // Defines a function named 'updateText'. This function can be called when an event (like a button click) occurs.

    document.getElementById("heading").innerHTML = "Games are Awesome";
    // Finds the HTML element with the ID 'heading' (your <h1> tag)
    // and changes its inner HTML content to display the text "Games are Awesome".
}
